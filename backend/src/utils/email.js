/**
 * Email service (Brevo + Nunjucks).
 *
 * Kept at this path for backwards compatibility — the implementation lives in
 * `../email/index.js` alongside the Nunjucks template directory.
 */
export { default } from '../email/index.js';